# 今後やりたいこと

やりたいことの控え。済んだものは消してある。

## 0. クラウド API 対応（実装済み・未検証）

`--engine deepgram / soniox / assemblyai / openai` で使える（`engines.py`）。
GPU が無い PC でも動かせる。**実際の API につないで確かめていない** ので、
最初に使うときは短い発話で試すこと。

残っていること:

- 実機での確認（キーを入れて短く試す）
- API キーをビューアから設定できるようにする（いまは環境変数のみ）
- どこに音声が送られているか画面に出す（ローカル完結でなくなるため）

### 懸念: 喋り始めが欠けるかもしれない

課金を抑えるため**発話ごとに接続・切断**している（1〜2文ごと）。
WebSocket の確立と認証に 200〜500ms かかるので、**最初の一言が欠ける**恐れがある。
ローカル版はモデルが常に載っているのでこの問題が無い。

実機で確かめて、実際に欠けるようなら次のどれかにする:

| 方式 | 利点 | 欠点 |
|---|---|---|
| いまの実装（発話ごと） | 課金が最小 | 喋り始めが欠ける恐れ |
| 接続を維持 | 取りこぼしなし | 黙っている間も課金される会社がある |
| 折衷（無音30秒で閉じる） | 連続して喋る間は保つ | やや複雑 |

いまは推測でしかないので、実際に困ってから直す。

選定の理由: 一日中マイクを開ける使い方なので、**接続時間で課金する会社**
（AssemblyAI は公式に明記、Deepgram も同様とされる）は不利。OpenAI は
音声の長さだけで課金するため、この用途に構造的に向いている。
Soniox は $0.12/時と安いが、日本語の精度は自社公表値しか無いので要検証。

## 1. Qwen3-ASR のまま他の環境で動かす

クラウド API（0番）は使えるようになったが、**ローカル完結のまま**
GPU 無し・Mac でも動かせるほうが、この道具の売りに合う。

**同じ Qwen3-ASR のまま、他の環境でも動かせる**（2026-07 時点で調査済み）:

| 環境 | 実装 | 必要なもの |
|---|---|---|
| Linux/Windows + NVIDIA | いまの vLLM 版 | VRAM 12GB |
| **Mac (macOS 26 以降)** | **実装済み** — OS 付属の `SpeechAnalyzer`（`engine_apple.py`、macOS では既定） | 追加ダウンロードもメモリ確保も無し |
| **Mac (macOS 25 以前)** | **実装済み** — `mlx-qwen3-asr`（`engine_mlx.py`） | Metal。メモリ約4GB |
| **GPU なし** | ONNX 版（`Daumee/Qwen3-ASR-0.6B-ONNX-CPU`） | CPU のみ。Intel N100 で RTF 0.71 の報告あり |
| GPU なし（Rust） | `qwen-asr` crate | Python も ONNX も不要 |

つまり **0.6B + ONNX なら安価なミニPCでもリアルタイムに間に合う**。
これを実装すればほぼ誰でも使えるので、公開時の価値が大きく変わる。

差し替え口は `asr_mic.py` の `load_model()` / `stream_utterances()` に
用意済み（`engines.py` と同じ形で足せる）。Mac 版はこの口に
`engine_mlx.py` を挿した（vLLM 版と同じ3メソッドを持つアダプタ。
KV キャッシュを使い回す増分デコードなので全音声の再投入も無い）。
残りは ONNX 版。

## 2. サービスとして提供する

スキルは公開して自由に使ってもらう。そのうえで、
「GPU を持っていないが音声で指示したい」層に向けた有料サービスを別に用意する。

- Aqua Voice のようなディクテーション用途も含める
- 課金はトークン制（使った分だけ）
- リモート GPU で推論し、結果だけ返す

**判断が要る点**: サービス版は音声をクラウドに送ることになる。
いまの「クラウドに音声を送らない」という売りと矛盾するので、
ローカル版とサービス版を明確に分けて示す必要がある。

## 3. 英語 README

この仕組みは日本語話者に限らず価値がある（ローカル完結の音声コーディング）。
コードは変更不要で、`README.en.md` を足すだけで届く範囲が広がる。

## 4. macOS / Windows の実機確認

録音経路のコードは書いたが未検証（`asr_mic.py` の `mic_command()`）。

- macOS: `ffmpeg -f avfoundation -i :0`
- Windows: `ffmpeg -f dshow -i audio=...`

実機を持っている人に試してもらうのが確実。

## 5. 常時表示のオーバーレイバー（見送り）

ブラウザを開かずに、認識中の内容が画面の端に常に出ている小さなバー。
検討したが、ブラウザのビューアで足りているため作っていない。

作るとしたら:

- **tkinter** — 追加導入なしで書ける（この環境に入っている）。見た目の作り込みは限界あり
- **Rust + GTK4** — 見た目も動作もよく、Wayland のレイヤーシェルで真のオーバーレイにできる。
  ただし `gtk4` と `gtk4-layer-shell` の導入（sudo）とビルドが必要。
  この環境では Rust 自体は入っているが GTK は未導入

## 6. 複数セッションで使う（見送り）

いまは発話ログが1つなので、Claude Code を2つ動かすと**同じ発話が両方に届く**。
「テストして」で両方が実行してしまう。デーモンは GPU を 12GB 使うため
セッションごとに立てることもできず、1つを共有するしかない。

作るとしたら宛先を選ぶ仕組みが要る:

- ビューアで送り先を切り替える（セッションごとにログを分ける）。誤実行がない
- 発話の先頭で宛先を言う（「フロント、テストして」）。手は使わないが誤認識で
  違うセッションに届くリスクがある

実際に複数使う場面が出てから考える。

## 7. 細かいもの

- 発話の履歴を日付ごとに保存する（いまは起動のたびに消える）
- 辞書を用途別に切り替える（プロジェクトごとの固有名詞）
- 認識結果の信頼度が低い発話に印を付ける
